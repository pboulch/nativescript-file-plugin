{
  "name": "tns-template-hello-world-ts",
  "main": "app.js",
  "version": "1.6.0",
  "description": "Demo project for the plugin",
  "license": "Apache-2.0",
  "android": {
    "v8Flags": "--expose_gc",
    "requireModules": [
      "nativescript-file-plugin"
    ],
    "markingMode": "none"
  },
  "devDependencies": {
    "nativescript-dev-typescript": "^0.3.0"
  },
  "scripts": {
    "build.plugin": "cd ../src && npm run build",
    "ci.tslint": "npm i && tslint --config '../tslint.json' 'app/**/*.ts' --exclude '**/node_modules/**'"
  },
  "directories": {}
}
